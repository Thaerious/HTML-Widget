import WidgetElement from "@html-widget/core";

class ${pascal} extends WidgetElement {

    constructor() {
        super("${dash}-template");
    }
}

window.customElements.define('${dash}', ${pascal});
export default ${pascal};