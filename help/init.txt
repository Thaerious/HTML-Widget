COMMAND
    init -  Create a new project in the root (current) directory.

SYNOPSIS
    npx widget init

DESCRIPTION
    1) Creates a .nigetrc file in the root directory with the default settings.  
    2) Creates the client-src directory.